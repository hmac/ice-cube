resolver: lts-9.3

packages:
- '.'
- location:
    git: git@github.com:hmac/business.git
    commit: 0b23b87
  extra-dep: true

extra-deps:
- dates-0.2.2.1

# Override default flag values for local packages and extra-deps
flags: {}

# Extra package databases containing global packages
extra-package-dbs: []
